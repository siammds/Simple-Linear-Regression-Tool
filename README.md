# Simple-Linear-Regression-Tool
Python GUI tool for Simple Linear Regression on CSV or Excel datasets. Select a file, choose independent (X) and dependent (Y) variables, and run regression instantly. Handles large datasets using chunk processing, computes R², MAE, RMSE, and generates a scatter plot with regression line and equation. Saves the plot automatically.
